<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Real-time Location Sharing & Google Maps -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MeetPinApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeetPin"
        tools:targetApi="36">

        <!-- Google Maps API Key Metadata -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!--
            launchMode="singleTop": 앱이 실행 중일 때 초대 링크를 탭하면 MainActivity를 새로
            만들지 않고 onNewIntent로 전달받는다. 기본값(standard)이면 인스턴스가 중복 생성된다.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MeetPin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links / Deep Link Filter for MeetPin Invites -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="meetpin.app"
                    android:pathPrefix="/invite" />
            </intent-filter>

            <!--
                커스텀 스킴은 별도 filter로 분리한다. 위 https filter에 합치면 스킴/호스트가
                교차 매칭되어 https://invite 같은 의도치 않은 URL까지 받게 된다.
                autoVerify는 커스텀 스킴에 적용되지 않으므로 여기에는 붙이지 않는다.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="meetpin"
                    android:host="invite" />
            </intent-filter>

            <!--
                출발 알림 탭 → 실시간 지도 딥링크 (meetpin://track/{groupId}).
                로컬 알림에서만 생성하는 내부 링크라 커스텀 스킴을 쓰며 autoVerify가 필요 없다.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="meetpin"
                    android:host="track" />
            </intent-filter>
        </activity>

        <!-- Location Tracking Foreground Service -->
        <service
            android:name="com.kero.meetpin.core.location.LocationTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

</manifest>
